What i can do now:

Quick file editing using vim/nano. Can use grep to read files based on filter options.

AI was used to expand on the new grep options and to break down what was wrong with the 
break/fix diagnostic activity.
