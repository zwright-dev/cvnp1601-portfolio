1.) You need to edit a config file on a server with no GUI. Walk me through it.

1A.) I would open it in nano or vim (probably nano) and edit it in the nano editor. and leave with ^X, click y and hit
enter to save it. 

2.) How do you tell which SSH settings are actually active, versus just documented in comments

2A.) By removing the comments by using grep filter commands. I would need to look to see which ones specifically. 

3.) A log file has one value per column but no header row. How do you pull out just the piece you need?

3A.) by using an awk command,I think the right fag is -f but i'm not certain.  

4.) Your manager wants a login report by end of day. How do you build it fast.

4A.) I do remember this one. You can use grep "Accepted password" in awk to put the log results into
a file to submit.

5.) Tell me about a time a command's output didn't match what you expected.

5A.) this activity. When I was trying to compile my access report and was trying to verify it with cat it kept coming back blank
I still am not sure if it is coirrect at all but I even copy and pasted them all instead of typing them to make sure I
didn't have a typo. But it kept coming back blank so I am not sure if it is correct or not.  

Note: Same thing for this set, I was still lost on where these questions are and was too stubborn to
ask for assistance. So again Il\'ll use these ones without the STAR section answers.


