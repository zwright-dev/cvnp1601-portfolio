1.) "You SSH into a server you've never seen. What are the first commands you run and why?"

1.A) I would execute pwd as well as a series of ls commands to both map the file system and confirm current working
directory.

2.) "A log file has 40,000 lines. How do you find the relevant entries?"

2.A) with filtered searching commands. The one that comes to mind for me is grep and its pipe filters

3.) "What's the difference between > and >>, and why does it matter?"

3.A) I don't remember the name, But the ONE arrow creates/overwrites an entry
 and the TWO arrows adds additional entries to the pre-existing file without an overwrite.

4.) "You run find and see dozens of 'Permission denied' lines. Is something broken?"

4.A) No you just are looking at protected paths and stderr will need to be redirected so you can read it instead of
receiving the permission denied result.

5.) "Tell me about a time you had to troubleshoot something unfamiliar."

5.A) I had to burn an ISO of Windows Server 2022 to a drive, and successfully get a standard
Windows Workstation to boot with that instead. I have in VM's several times but this was new.
I think I stayed at friends shop for 10 hours until I was able to configure the drive to boot
it properly after a ton of trial and error. 

Note: I wasn't entirely certain that these were the correct questions for this, but I think its batter than leaving a blank file
for submission so I took my best shake at doing this without looking the STAR steps.
